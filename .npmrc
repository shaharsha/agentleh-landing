# vite-react-ssg declares vite peer @"<=7" but works fine with vite@8.
# Paraglide JS + vite-react-ssg + Tailwind 4 + React 19 all align; this
# just suppresses the incorrect-by-a-version peer warning from blocking
# install. Revisit once vite-react-ssg bumps its peer range.
legacy-peer-deps=true
